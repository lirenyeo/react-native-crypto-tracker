import React from 'react'
import { View, StyleSheet } from 'react-native'

const AltfolioScene = () => (
  <View style={container}>
  </View>
)

const {
  container
} = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'cornflowerblue',
  }
})

export default AltfolioScene